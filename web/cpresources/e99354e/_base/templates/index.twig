{% set entry = craft.entries.slug('home').one() %}
{% set requireLogin = entry['requireLogin'] ?? false %}

{% embed '_layouts' %}
    {% if requireLogin %}
        {% requireLogin %}
    {% endif %}
{% endembed %}
