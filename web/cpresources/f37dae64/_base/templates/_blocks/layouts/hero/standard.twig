{% import '_utilities/format' as format %}
{% import '_utilities/transforms' as transform %}



<article class="hero-standard hero-standard--full section-padding relative" data-hero>
	<div class="hero-standard__int wrapper">
	
		{{ transform.getImage({
			'asset': entry.backgroundImage|default,
			'transform': 'hero',
			'to-lg': 'heroLg',
			'to-sm': 'heroSm',
			'wrapperClasses': "hero-standard__image-wrapper",
			'classes': "hero-standard__image object-fit",
			'placeholder': true
		}) }}

		<div class="hero-standard__info bg--dark">
			<div class="hero-standard__info-int">
				{% if entry.heading|default %}   
					<p class="hero-standard__heading faux-h2">{{ format.basic(entry.heading) }}</p>
				{% endif %}
				
				{% if entry.intro|default %}
				{{entry.intro|retconAttr('p', { class: 'faux-p-lg hero-standard__intro' })}}
				{% endif %}

				{% include '_blocks/buttons' with { entry: entry, options: {classes:'btn btn--arrow btn--sm hero-standard__btn'}  } only %}
				
				{#
				{% if entry.linkUrl|default %}
					{{format.getLink(entry.linkUrl,entry.linkText,'btn btn--arrow btn--sm hero-standard__btn')}}
				{% endif %}
				#}
				
			</div>
		</div>
	</div>
</article>
