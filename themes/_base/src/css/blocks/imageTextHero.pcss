/* Layout lives in the shared pin-section utility (includes/pins.pcss); the
   template's __int/__items/__info hooks are styled there, not here. */
.image-text-hero{
    position: relative;
}

.image-text-hero__image-wrapper{
    width: 100%;
    height: 100%;
}
