{#
	reCAPTCHA v3 — invisible, score-based. Include once per page that has a
	form; front-end JS still needs to call grecaptcha.execute() before
	submit to populate each form's hidden recaptchaToken field (not yet
	wired up). Verified server-side in modules/contactform/Module.php.
#}
{% set recaptchaSiteKey = recaptchaSiteKey() %}

{% if recaptchaSiteKey %}
<script src="https://www.google.com/recaptcha/api.js?render={{ recaptchaSiteKey }}"></script>
<script>
	window.recaptchaSiteKey = {{ recaptchaSiteKey|json_encode|raw }};
</script>
{% endif %}
