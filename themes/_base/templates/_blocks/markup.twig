{% set isLast = last ?? false %}		
{% set parent = parent ?? 'default' %}

{% set defaultAttr = {
    class: [
        'text-block',
        'section section--sm',
        'prose',
        isLast ? 'section--last',
    ]
} %}

<article {{attr(defaultAttr)}}>
    <div class="text-block__int {{ parent != 'column' ? 'container-text'}}">
        {{ chunk|retconAttr('ul, ol', { role: 'list' }) }}
    </div>
</article>